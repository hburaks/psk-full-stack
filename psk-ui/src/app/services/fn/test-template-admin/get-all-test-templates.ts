/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TestTemplateResponse } from '../../models/test-template-response';

export interface GetAllTestTemplates$Params {
}

export function getAllTestTemplates(http: HttpClient, rootUrl: string, params?: GetAllTestTemplates$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TestTemplateResponse>>> {
  const rb = new RequestBuilder(rootUrl, getAllTestTemplates.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TestTemplateResponse>>;
    })
  );
}

getAllTestTemplates.PATH = '/v2/admin/test-templates';
